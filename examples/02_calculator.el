ALGORITHM calculator {
    show "==== El Calculator Demo ====";
    
    var a: integer = 10;
    var b: integer = 5;
    
    show "a = " + a;
    show "b = " + b;
    show "a + b = " + (a + b);
    show "a - b = " + (a - b);
    show "a * b = " + (a * b);
    show "a / b = " + (a / b);
}